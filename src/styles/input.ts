import { StyleSheet } from "react-native";

export const commonInput = StyleSheet.create({
  authContainer: {
    flexDirection: "row",
    height: 55,
    borderRadius: 25,
  },
  taskCreatorContainer: {
    flexDirection: "row",
    height: 60,
    marginVertical: 10,
  },
});
